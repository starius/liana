[package]
name = "musig2_signer"
version = "0.1.0"
edition = "2021"

[dependencies]
miniscript = { version = "12.0", features = ["base64"] }
musig2 = { version = "0.3.1", default-features = false, features = ["secp256k1"] }
getrandom = "0.3.1"
liana = { path = "../../../liana" }

# Avoid it being built under Liana's target dir.
[workspace]
